package dev.railroadide.railroad.ui.styling;

/** Size presets mapped to CSS classes by Railroad text input controls. */
public enum TextFieldSize {
    /** Compact input dimensions and padding. */
    SMALL,
    /** Default input dimensions and padding. */
    MEDIUM,
    /** Larger input dimensions and padding. */
    LARGE
}
